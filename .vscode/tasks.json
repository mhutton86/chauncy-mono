{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build Chauncy Spring Boot App",
      "type": "shell",
      "options": {
        "cwd": "${workspaceFolder}/chauncy-service"
      },
      "command": "./gradlew build --info -x test",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "detail": "Generated task for building the project using Gradle"
    }
  ]
}
